# 饮品选择 - 调试指南

## 🐛 问题排查步骤

### 如果饮品选项不显示，请按以下步骤检查：

1. **打开浏览器控制台** (F12)
2. **刷新页面** 
3. **进入游戏，选择角色后进入饮品选择页面**
4. **查看控制台输出**

### 可能的情况：

#### 情况A：看到 "找不到 DrinkItems 容器"
- 原因：HTML元素不存在或ID错误
- 解决：检查 `index.html` 第90-105行

#### 情况B：看到 "DRINKS is not defined"
- 原因：`drinkSystem.js` 没有正确加载
- 解决：检查 `index.html` 中脚本顺序

#### 情况C：页面空白，没有任何内容
- 原因：JavaScript报错中断
- 解决：查看控制台具体错误信息

#### 情况D：看到 "请选择一款饮品！点击卡片即可选择"
- ✅ 正常！饮品系统已加载
- 如果还是看不到饮品选项，检查CSS

---

## ✅ 正确的流程应该是：

```
1. 打开网页
2. 点击"开始游戏"
3. 选择"签证官"或"申请人"
4. → 进入道具商店（购买或不购买都可以）
5. → 自动跳转到饮品选择页面
6. → 应该看到6个饮品卡片排列
7. → 点击任意一个会有选中效果（蓝色边框）
8. → 聊天系统显示"已选择 XXX"
9. → 点击"确认选择"按钮
```

---

## 🔧 临时解决方案

如果还是无法选择饮品，可以：

1. **清除浏览器缓存**：
   - Chrome/Edge: Ctrl+Shift+Delete
   - 选择"缓存的图片和文件"
   - 点击"清除数据"

2. **强制刷新**：
   - Windows: Ctrl+F5
   - Mac: Cmd+Shift+R

3. **检查浏览器是否阻止脚本**：
   - 查看地址栏右侧是否有图标
   - 如果有，点击并允许脚本运行

---

## 📱 检查清单

- [ ] 控制台没有红色错误信息
- [ ] `DrinkSystem.renderDrinks()` 被调用
- [ ] DRINKS 数组有6个元素
- [ ] #DrinkItems DOM 元素存在
- [ ] 饮品卡片 HTML 正确生成
- [ ] CSS 类 `.drink-card` 正确应用
- [ ] 点击饮品卡片有选中效果
- [ ] 聊天记录显示"已选择"消息

---

## 💡 常见问题

### Q: 点饮品没反应？
A: 确保先初始化音频（点击页面任意位置）

### Q: 选完饮品点后确认按钮是灰色？
A: 确认按钮始终可以点击，灰色只是样式问题

### Q: 饮品选项是空的但没报错？
A: 可能是 DRINKS 数组为空，检查 drinkSystem.js 是否加载

---

**修复时间**: 2026年9月11日  
**修改文件**: 
- ✅ `public/js/drinkSelect.js` - 添加event参数，移除Object.assign
- ✅ `public/js/dialogue.js` - 移除Object.assign
- ✅ `public/js/decision.js` - 移除Object.assign
- ✅ `public/js/materialReview.js` - 已有检查
