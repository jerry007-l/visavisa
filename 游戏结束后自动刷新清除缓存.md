# 🔄 游戏结束后的自动重置

**首次实现**: 2026年9月11日（3 秒 + `window.location.reload()`）
**当前实现**: 2026年9月12日（8 秒 + `Game.backToMain()`）
**需求**: "每轮游戏后自动刷新程序，清除之前的缓存"

> ⚠️ **本文档已按当前代码重写。**
> 旧版写的是"3 秒后整页刷新"，那套实现已经被替换掉了。文件名里的"自动刷新清除缓存"
> 是历史叫法，现在既不整页刷新、也不清浏览器缓存 —— 清的是**本局游戏状态**。
> 如果要改名，建议叫 `游戏结束后自动重置.md`。

---

## 🎯 功能概述

结算界面停留 **8 秒**后自动回到主菜单，并把上一局的状态清干净。

1. ✅ **8 秒后自动返回主菜单** —— 给玩家看完结算、身份揭示和成就提示的时间
2. ✅ **倒计时 + 进度条** —— 两者由同一个定时器驱动，永远同步
3. ✅ **手动立即返回** —— 按钮可以跳过等待
4. ✅ **显式清理本局状态** —— `Game.resetGame()` + `Chat.reset()`，不依赖整页刷新
5. ✅ **跨局进度完整保留** —— 金币、成就、统计数据不受影响

---

## 🔧 技术实现

涉及三个文件：`public/js/result.js`（倒计时）、`public/js/main.js`（重置）、`public/js/chat.js`（清聊天记录）。

### 1️⃣ 结算界面的倒计时 UI — `result.js`

```javascript
<div class="auto-refresh-timer" style="margin-top: 20px; text-align: center;">
    <p style="color: #7F8C8D; font-size: 0.9em;">
        🔄 <span id="RestartCountdown">${this.AUTO_RESTART_MS / 1000}</span> 秒后自动返回主菜单…
    </p>
    <div style="width: 200px; height: 6px; background: #ECF0F1; border-radius: 3px; margin: 10px auto;">
        <div id="refreshProgressBar" style="width: 0%; height: 100%;
            background: linear-gradient(90deg, #3C3B6E 0%, #B22234 100%);
            border-radius: 3px;"></div>
    </div>
    <button class="btn-primary" onclick="Game.backToMain()" style="margin-top: 15px;">
        🏠 立即返回主菜单
    </button>
</div>
```

这两个元素是在 `Result.show()` 里随结算内容一起生成的，紧接着调用 `startAutoRestart()`
去读它们，所以顺序是安全的。

---

### 2️⃣ 倒计时与进度条 — `result.js`

```javascript
const Result = {
    AUTO_RESTART_MS: 8000,

    startAutoRestart() {
        clearInterval(this.restartInterval);
        clearTimeout(this.restartTimeout);

        const total = this.AUTO_RESTART_MS;
        const startedAt = Date.now();
        const progressBar = document.getElementById('refreshProgressBar');
        const countdown = document.getElementById('RestartCountdown');

        this.restartInterval = setInterval(() => {
            const elapsed = Date.now() - startedAt;
            const ratio = Math.min(1, elapsed / total);
            if (progressBar) progressBar.style.width = `${ratio * 100}%`;
            if (countdown) countdown.textContent = Math.max(0, Math.ceil((total - elapsed) / 1000));
            if (ratio >= 1) clearInterval(this.restartInterval);
        }, 50);

        this.restartTimeout = setTimeout(() => {
            console.log('🔄 结算结束，返回主菜单');
            Game.backToMain();
        }, total);
    },

    cancelAutoRestart() {
        clearInterval(this.restartInterval);
        clearTimeout(this.restartTimeout);
    }
};
```

**为什么这么写**：

| 设计点 | 原因 |
|--------|------|
| 进度条按 `elapsed / total` 算，不累加 | 旧版是 `progress += 2` 每 30ms 累加，标签写"3 秒完成"，实际算下来是 4.5 秒，进度条走完了倒计时还没到。按真实耗时算就不会漂移。 |
| 数字和进度条共用一个 `setInterval` | 两个定时器各走各的，迟早对不上 |
| 用 `Date.now()` 而不是数拍子 | 浏览器后台标签页会把定时器节流到 1 秒一次，数拍子会算出远小于真实值的进度 |
| 先 `clearInterval` / `clearTimeout` 再启动 | 玩家连开两局时，上一局的定时器不能还在跑 |
| `Math.max(0, Math.ceil(...))` | 最后 100ms 内不该闪出 "-1" |
| `Math.min(1, ratio)` | 同理，进度条不该超过 100% |

---

### 3️⃣ 回到主菜单时清掉上一局 — `main.js`

```javascript
backToMain() {
    if (typeof Result !== 'undefined' && Result.cancelAutoRestart) {
        Result.cancelAutoRestart();      // 玩家手点按钮时，别再让倒计时把他弹一次
    }
    if (typeof Dialogue !== 'undefined') {
        clearInterval(Dialogue.timer);   // 答题限时器
        Dialogue.timer = null;
    }
    this.resetGame();
    this.showScreen('mainMenu');
    this.updateCoinDisplay();
},

resetGame() {
    Object.assign(this.state, {
        playerRole: null,
        currentRound: 1,
        applicantIdentity: null,
        drawnMaterials: [],
        questionIndex: 0,
        score: 70,
        dialogueHistory: [],
        materialFake: {},
        randomEventTriggered: [],
        gameStartTime: null,

        usedItemIds: [],
        itemHistory: [],
        skipNextQuestion: false,
        autoAnswerRounds: 0,
        timeBonus: 0,
        timeBonusRounds: 0,
        lenientScoring: false,
        suspicionRadar: false,
        pressureMode: false,
        revealedClues: [],
        materialRevealed: []
    });

    this.clearTimer();               // 全局计时器（商店限时等）
    Shop.selectedItems = [];         // 商店道具选择

    // 整页刷新已经换成 backToMain()，这些残留得自己清
    Chat.reset();
    if (typeof ItemQuickUse !== 'undefined' && ItemQuickUse.hide) {
        ItemQuickUse.hide();
    }

    console.log('🔄 游戏已重置');
}
```

`Dialogue.init()` 自己会重置 `questions` / `questionIndex` / `currentQuestion` /
`currentAiAnswer` / `awaitingEventReturn`，所以题目残留不用在这里管。

---

### 4️⃣ 清空聊天记录 — `chat.js`

```javascript
// 开局前清空上一局的聊天记录：addMessage 只追加，50 条上限也只裁数组不删 DOM，
// 页面不再整页刷新，所以必须显式清一次，否则新局里还看得到上局的对话
reset() {
    this.messages = [];
    const container = document.getElementById('ChatMessages');
    if (container) container.innerHTML = '';
},
```

**这是换成 `backToMain()` 之后必须补的一块。** `Chat.addMessage()` 只做两件事：
往 `#ChatMessages` 里 `appendChild`，往 `this.messages` 里 `push`。那个"只保留最近 50 条"
的上限只 `shift()` 数组，**从不删 DOM 节点**。整页刷新时这一切都被浏览器抹掉了，
不刷新之后就会：上一局的对话原样留在新局里，而且 DOM 节点无上限地一直涨。

---

## 📊 完整流程

```
┌──────────────────────────────┐
│  游戏结束，显示结算界面       │
│  🎉 判断正确！/ ❌ 判断失误  │
└──────────────────────────────┘
              ↓
┌──────────────────────────────┐
│  统计数据 + 申请人真实身份    │
│  + 本局新解锁的成就           │
└──────────────────────────────┘
              ↓
┌──────────────────────────────┐
│  🔄 8 秒后自动返回主菜单…     │
│  ██████████░░░░░░░░░░  5     │
│  [🏠 立即返回主菜单]          │
└──────────────────────────────┘
              ↓
      ┌─────────────┐          ┌─────────────┐
      │  等满 8 秒   │   或者   │  玩家点按钮  │
      └─────────────┘          └─────────────┘
              ↓
┌──────────────────────────────┐
│  Game.backToMain()           │
│  1. Result.cancelAutoRestart │
│  2. clearInterval(Dialogue)  │
│  3. Game.resetGame()         │
│  4. showScreen('mainMenu')   │
│  5. updateCoinDisplay()      │
└──────────────────────────────┘
              ↓
┌──────────────────────────────┐
│  主菜单，金币和成就都还在     │
└──────────────────────────────┘
```

---

## 🧹 到底清了什么、没清什么

换成 `backToMain()` 之后，"清除缓存"这件事必须说清楚 —— 它不是浏览器的缓存。

### 清掉的（本局状态，全部在内存里）

| 类别 | 具体内容 |
|------|----------|
| 对局 | `playerRole`、`applicantIdentity`、`currentRound`、`gameStartTime`、`score`（回到 70） |
| 问答 | `dialogueHistory`、`questionIndex`，以及 `Dialogue` 自己的题库和当前题 |
| 材料 | `drawnMaterials`、`materialFake`、`materialRevealed` |
| 道具 | `usedItemIds`、`itemHistory`，以及 8 个效果开关（`skipNextQuestion`、`autoAnswerRounds`、`timeBonus`、`timeBonusRounds`、`lenientScoring`、`suspicionRadar`、`pressureMode`）、`Shop.selectedItems` |
| 线索 | `revealedClues` |
| 事件 | `randomEventTriggered` |
| 定时器 | `Game.state.timer`、`Dialogue.timer`、`Result.restartInterval`、`Result.restartTimeout` |
| 界面 | `#ChatMessages` 的 DOM 与 `Chat.messages`、道具背包面板 |

### 故意不清的（跨局进度，存在 `localStorage` 的 `visaGame` 键里）

| 类别 | 具体内容 |
|------|----------|
| 经济 | `coins` —— 这是核心循环，清了等于每局都从 200 重来 |
| 成就 | `achievements` |
| 统计 | `stats`：`gamesPlayed`、`wins`、`losses`、`officerStreak`、`applicantStreak`、`fraudstersCaught`、`randomEvents`、`chatMessages` 等 |
| 去重记录 | `seenQuestions`、`seenEvents`、`purchasedItems`、`questionCounts` |

18 个成就里有 11 个（`first-game`、`fire-eyed`、`rich-list`、`laugh-out-loud`、
`officer-legend`、`applicant-legend`、`question-veteran`、`event-hunter`、
`item-collector`、`replay-king`、`chat-master`）读的是跨局累计数据。
**这些数据一旦在重置时被清掉，那些成就就永远解锁不了。**

---

## ⚠️ 为什么不再用 `window.location.reload()`

旧版方案对比表里，`location.reload()` 的唯一缺点是"会闪白屏"。实际上问题不止这一个：

| | `location.reload()` | `Game.backToMain()` |
|---|---|---|
| 白屏闪烁 | 有 | 无 |
| 重新解析 22 个脚本 + 5 个样式表 | 是 | 否 |
| 打断背景音乐 | 是（`AudioContext` 重建，还要玩家再点一次才恢复） | 否 |
| 清 `localStorage` | **否** | 否 |
| 清内存里的 JS 状态 | 是（顺带的） | 是（`resetGame()` 显式做） |
| 能被无头测试验证 | 不能 —— 测试环境里刷新等于把测试自己也刷掉 | 能 |

**关键一点：`location.reload()` 从来没有清过 `localStorage`。**
旧文档说它"清除所有缓存"，但玩家的金币、成就、统计一直都在 `localStorage` 里，
刷新前后毫无变化。它真正清掉的只有内存里的 JS 变量 —— 而这正是 `resetGame()`
现在逐字段显式清掉、并且能被 `npm run test` 断言覆盖的部分。

另外，本项目有 Service Worker，`fetch` 是 cache-first（`sw.js`）：

```javascript
self.addEventListener('fetch', (event) => {
    event.respondWith(
        caches.match(event.request).then((response) => response || fetch(event.request))
    );
});
```

刷新拿到的是缓存里的旧文件，**不会**回源。所以"刷新一下拿到最新代码"这个直觉在这个项目里
本来就不成立 —— 要让老玩家拿到新代码，得改 `sw.js` 里的 `CACHE_NAME`
（当前 `visa-game-v5`），`activate` 事件会顺手删掉所有旧版本的缓存。

---

## 🎨 UI 设计细节

### 进度条

```css
/* 轨道 */
width: 200px;
height: 6px;
background: #ECF0F1;
border-radius: 3px;

/* 填充 */
background: linear-gradient(90deg, #3C3B6E 0%, #B22234 100%);
border-radius: 3px;
```

深蓝 `#3C3B6E` → 红 `#B22234`，与游戏整体的美国签证主题配色一致。

填充条**没有** CSS `transition`。旧版加了 `transition: width 0.1s linear` 是因为它每 30ms
跳一次、需要靠过渡抹平；现在每 50ms 按真实耗时设置一次宽度，本身就是平滑的，
再加过渡只会让进度条永远慢半拍。

### 按钮

沿用已有的 `.btn-primary`：蓝色渐变、悬停上浮。文案是"🏠 立即返回主菜单"，
不说"开始新局"，因为 `backToMain()` 落在主菜单，玩家还要再点一次选角色。

---

## 🎯 边界情况

### 1. 玩家在倒计时结束前自己点了按钮

`Game.backToMain()` 第一件事就是 `Result.cancelAutoRestart()`，把 `restartTimeout` 清掉，
所以不会返回主菜单之后又被 8 秒定时器弹一次。

### 2. 玩家在 8 秒内关掉页面

定时器随页面一起消失，没有副作用。

### 3. 玩家连开多局

`startAutoRestart()` 开头先 `clearInterval` / `clearTimeout`，不会有多个定时器叠加。

### 4. 标签页切到后台

浏览器会把定时器节流。因为进度条按 `Date.now()` 的真实差值计算，切回来时进度条和
倒计时会一次性跳到正确位置，然后正常触发返回 —— 不会卡在某个中间值。

### 5. 调试（F12）

`console.log('🔄 结算结束，返回主菜单')` 和 `resetGame()` 里的
`console.log('🔄 游戏已重置')` 标记了两个时间点。

---

## 🚀 可能的后续调整

### 1. 改等待时长

只动一个常量：

```javascript
const Result = {
    AUTO_RESTART_MS: 8000,   // 倒计时文字、进度条、定时器都从这里读
```

UI 上的秒数写的是 `${this.AUTO_RESTART_MS / 1000}`，所以改这一个数字，
文案、进度条、实际延时三处会一起变，不会出现旧版那种"写着 3 秒实际 4.5 秒"的错位。

8 秒的取值依据：结算界面现在比旧版多了"申请人真实身份"和"本局新解锁成就"两块内容，
3 秒读不完；但超过 10 秒，想连开的玩家会觉得被拖住。

### 2. 按结局区分时长

```javascript
const delayMap = { success: 8000, failure: 10000 };
Result.AUTO_RESTART_MS = delayMap[result.success ? 'success' : 'failure'];
```

失败局多给两秒消化。注意要在 `show()` 里、`startAutoRestart()` 之前赋值。

### 3. 最后几秒可以取消

再加一个"留在本页"按钮，调 `Result.cancelAutoRestart()` 即可，
不需要新的状态字段。

### 4. 记录本局数据到 localStorage

其实已经在做了 —— `Game.calculateResult()` 会把本局结果写进 `UserData`，
成就和统计都读它。如果要额外存一份"最后一局详情"用于展示，
应该在 `Result.show()` 里写，而不是在刷新前抢救。

---

## 📊 性能影响

| 阶段 | 影响 | 说明 |
|------|------|------|
| 等待 8 秒 | 极低 | 一个 50ms 的 `setInterval`，每次改两个 DOM 属性 |
| 返回主菜单 | 极低 | 切 CSS class + 重置一个对象的字段，没有网络请求、没有重新解析 |
| 对比整页刷新 | 省下 27 个资源的重新解析 | 22 个脚本 + 5 个样式表 |

内存方面，不刷新意味着页面长期存活，所以**任何新增的每局状态都必须挂进
`resetGame()`**，否则会跨局累积。目前已知的两处（聊天记录、道具面板）都已经处理，
`tools/selftest.js` 里有断言盯着。

---

## ✅ 测试清单

### 自动化

`npm run test`（`node tools/selftest.js`，无需 `npm install`）里覆盖了：

- [x] `result.js` 不含 `location.reload`
- [x] `backToMain()` 后 `state.screen === 'mainMenu'`
- [x] `Chat.messages` 被清空
- [x] `#ChatMessages` 的 DOM 被清空
- [x] `playerRole` / `dialogueHistory` / `drawnMaterials` / `usedItemIds` /
      `materialRevealed` / `revealedClues` / `applicantIdentity` 全部复位
- [x] `score` 回到 70、`currentRound` 回到 1、`Shop.selectedItems` 清空
- [x] `coins` **没有**被重置（经济跨局保留）
- [x] 推进 2 分钟虚拟时间后不会被遗留定时器劫持回商店
- [x] 商店计时器已在 `confirmPurchase` 时清除

跑法：

```bash
npm run check      # 数据校验 + 无头跑测，当前 102 条断言全过
npm run test       # 只跑无头跑测
npm run test:data  # 只跑数据校验
```

### 手工（浏览器里还没验过）

- [ ] 结算界面显示"8 秒后自动返回主菜单"
- [ ] 倒计时数字与进度条同步走完
- [ ] 8 秒后自动回到主菜单
- [ ] 点"🏠 立即返回主菜单"立刻回去，且不会被再弹一次
- [ ] 连开两局，第二局的聊天区是干净的
- [ ] 连开两局，金币和成就都还在
- [ ] F12 控制台能看到 `🔄 结算结束，返回主菜单` 和 `🔄 游戏已重置`

> 手工这部分目前**没有验证过**：开发环境里起不了本地服务，浏览器自动化也被拦了。
> 想自己验的话，`node tools/serve.js 3000` 然后开 http://localhost:3000。

---

## 💡 经验

### 1. 别用累加值画进度条

按真实经过的时间算比例，标签、动画、实际延时三者天然一致；
累加固定步长则会随着定时器节流、步长取整慢慢漂开。

### 2. 一个定时器驱动一组联动的 UI

倒计时文字和进度条是同一个事实的两种表现，就该由同一个 tick 更新。

### 3. "刷新页面"不是清理手段

它清不掉 `localStorage`，在有 Service Worker 的项目里也拿不到新代码，
代价却是白屏、重新解析和音乐中断。要清什么，就显式清什么 —— 顺便还能被测试盯住。

### 4. 去掉整页刷新之后，要盘一遍"以前靠刷新顺手清掉的东西"

这次漏出来的是聊天记录（DOM 无限增长）和道具面板。
以后每加一个"每局会脏"的全局状态，都得同步进 `resetGame()`。

---

## 📝 总结

**当前行为**：结算界面停 8 秒 → 自动回主菜单；期间倒计时数字和进度条同步走完；
玩家可以点按钮立刻回去。

**清理方式**：`Game.resetGame()` 显式复位 21 个本局字段 + `Shop.selectedItems`
+ `Chat.reset()` + `ItemQuickUse.hide()` + 4 个定时器。金币、成就、统计、
去重记录一律保留 —— 它们是跨局进度，也是 11 个成就的判定依据。

**相比旧版的改动**：不再 `window.location.reload()`。省掉白屏和 27 个资源的重新解析，
不再打断音乐，而且清理范围第一次变得明确、可测。
